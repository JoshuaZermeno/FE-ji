There is a buck for <%= ::Employee.find(@buck.employee_id).full_name %> issued by <%= ::Employee.find(@buck.issuer_id).full_name %> from <%= ::Department.find(@buck.department_id).name %> that is awaiting your approval.

To approve this buck, please go to <%= @url %>